# Agent Pipeline Engine — 全量上下文汇总

> 整理日期：2026-08-22
> 整理人：架构师
> 范围：编排基座（Dispatcher + 证据门禁 + Agent PULL）的全部材料、事实与上下文

---

## 0. 一句话定位

CTGC 的**编排基座**，补上三基座缺失的那块（运行时 Hermes ✅ / 通信 A2A ✅ / 编排 ← APE）。

核心主张：

> **确定性控制面 + 非确定性智能 + 可验证证据 + 持久记忆**
> Agent 只当执行节点、不直接对话，靠状态机 + 证据门禁 + 人类任意点介入。

APE 踩过「A2A 同步请求-回复 vs LLM 异步长思考」这条死路后，收敛到的唯一生产验证路线。

---

## 1. 真实状态（⚠️ 最易踩坑，务必先读）

| 维度 | 事实 |
|---|---|
| **"v5"** | 是**提案 5 轮迭代**（v1→v5 经 BA-01/DEV-01/ORC-01 评审签字），**不是软件版本号** |
| **软件版本** | 始终 **v0.1**（git log: `init` / `reboot: v0.1`） |
| **本地 git 仓库** | `~/agent-pipeline-engine/` 停在 v0.1 = **A2A 死代码**（含"不确认"被误判为 confirmed 的关键词自报 bug） |
| **真实部署** | Dispatcher（FastAPI + 15 状态 + 证据门禁）在 **CI-01 `/opt/pipeline/`** |
| **真实代码位置** | **只在 CI-01，唯一裸代码副本，无 `.git`**（2026-08-22 实盘核查） |
| **DEV-01 节点** | 无 pipeline 源码（已核查：`~/pipeline`、`/opt/pipeline` 全空） |
| **风险等级** | 🔴 **唯一真实副本零版本管理**——CI-01 磁盘故障即永久丢失 |

### 结论

1. push GitHub 前**必须先 commit CI-01 的 Dispatcher 真实代码**，否则公开的是 v0.1 死代码。
2. 更紧急的是：**CI-01 上的代码现在没有任何备份**，先备份（scp 回本机 git）再谈开源。

---

## 2. 架构

```
CI-01 (123.56.25.232:8800) — pipeline.db 独占写（SQLite WAL）
  ↓ HTTP API（Bearer Token + IP 白名单）
Agent 节点（5 ECS）— cron 3min 心跳 + poll + claim + PATCH 证据
架构师 — ctgc CLI（approve/cancel/advance）
```

### 核心文件（CI-01 `/opt/pipeline/`）

| 文件 | 大小 | 用途 | 最后修改 |
|------|:--:|------|------|
| `main.py` | 18KB | FastAPI + dispatch_loop + auth | 8/12 14:11 |
| `gate_judge.py` | 2.8KB | PASS/BLOCKED 判定（15 状态） | 8/11 22:09 |
| `models.py` | 3.5KB | SQLite DDL + 数据层 | 8/11 19:22 |
| `config.json` | 990B | Token + IP 白名单 | 8/11 17:40 |
| `init_db.py` | 197B | 初始化 | 8/11 15:23 |

---

## 3. 通信模型（关键设计）

- **Agent PULL，非 PUSH**：Agent 每 3 分钟 poll Dispatcher API，claim 任务，在自己 Hermes 会话执行，PATCH 证据回写。
- **A2A 降级**（2026-08-11）：A2A 同步请求-回复与 LLM 异步/非确定性/长思考根本 mismatch。A2A 降为**辅助通道**（人类对话 + 诊断 + 可选唤醒），任务内容不走 A2A。
- **DB 独占写**：SQLite 唯一真相源，Dispatcher 独占写，Agent 只读自己相关行。

---

## 4. 状态机（15 状态）

```
received → ears_draft → waiting_human → ready → dev_working → qa_working → demo_working → done
          + blocked / cancelled / escalate_human / timeout
```

- 人类状态免超时：`waiting_human`、`escalate_human`
- 超时降级链：RETRY(30s→60s→120s) → SKIP(P2 only) → ROLLBACK(≤3) → ESCALATE_HUMAN

---

## 5. 证据门禁（五站，核心价值）

| 门禁 | 证据 | 校验 |
|------|------|------|
| BA done | `ears_doc_path` + `signed_by` | 非空 |
| ORC done | `orc_doc_path` + `dev/qa_confirmed=1` | 非空 + 双确认 |
| DEV done | `git_commit` + `pytest_report` + `SHA256` | 全非空 |
| QA done | `test_report` + coverage≥60 + blocking=0 | 非空 + 阈值 |
| DEMO done | `deploy_url` + `health_check_ok` | HTTP 200 活体探针 |

**设计原则**：`Completed = f(State, Evidence, Policy)`，**不等于 Agent 自报**。DEMO 假完成会被硬活检当场打回。

---

## 6. ctgc CLI

```bash
ctgc create <id> <project>    # 启动任务
ctgc approve <id> [by]        # 签署 EARS
ctgc cancel <id>              # 取消
ctgc advance <id> <status>    # 强制推进
ctgc agents                   # 在线状态
```

- 安装：`/usr/local/bin/ctgc`（架构师本机）
- 认证：`CTGC_API_TOKEN`
- 签署端点陷阱：是 `POST /tasks/{id}/approve`，**不是 `/sign`**

---

## 7. 部署记录（CI-01）

- IP：`123.56.25.232`，Alibaba Cloud Linux 3，2C/3.5G/49G
- Python 3.11.15，venv `/opt/pipeline/venv/`
- SSH key：`~/.ssh/ctgc-ci-01.pem`
- systemd：`pipeline-dispatcher.service`（:8800 常驻）+ `pipeline-dispatcher-loop.timer`（30s oneshot）
- DB：`/opt/pipeline/pipeline.db`（WAL，4 表）
- 文档存储：`/opt/pipeline/docs/`
- Phase 1 验收：5/5（health / schema / systemd / heartbeat / document API）

### Agent cron 部署

各节点每 3 分钟跑 `agent_poll.sh`：DEV-01（自部署）/ QA-01 / DEMO-01 / BA-01（admin 用户）/ ORC-01。

---

## 8. 关键文档索引

| 文档 | 路径 | 内容 |
|------|------|------|
| PROPOSAL-v5 全文 | `~/agent-pipeline-engine/PROPOSAL-v5.md` | v5 架构定稿 |
| ADR-001 | `~/agent-pipeline-engine/docs/adr/20260815_ADR-001-deterministic-control-plane.md` | 确定性控制面五边界 + 9 判断 + 学习环三表 |
| 仓库-部署同步差距 | skill `references/repo-sync-gap.md` | v0.1 vs Dispatcher 不同步根因 |
| 架构方向 | skill `references/architecture-direction.md` | 五边界分离 |
| CI-01 部署记录 | skill `references/ci-01-deployment.md` | Phase 1 验收 + cron + 白名单 |
| 主 SKILL | `~/.hermes/skills/devops/agent-pipeline-engine/SKILL.md` | 部署诊断手册 + 踩坑 |

---

## 9. 确定性控制面架构（ADR-001 核心）

五边界（必须保持分离）：

| 边界 | 性质 | 定义 |
|------|------|------|
| Execution | 确定性 | State + Policy → Action |
| Intelligence | 非确定性 | Input + Context → Artifact |
| Verification | 独立 | Artifact + Independent Evidence → 验证结果 |
| Learning | 非确定性 | Episodes + Failure Patterns → Policy Proposal |
| Promotion | 确定性 | Policy Proposal + Gates → Active Policy |

9 条关键判断（摘要）：

1. Multi-Agent 价值 = 独立信息增益 `I(Y;X₂|X₁)`，非 Agent 数量。
2. 验证比"错误损失"便宜：`EV = Error Reduction × Error Cost − Coordination Cost`。
3. 先问能否确定性验证（pytest/rule engine），不能才加独立推理节点。
4. 独立性单位 = Context Boundary，非 Agent 数量。
5. "独立"降级为"去相关"（Error Decorrelation）：verifier 不复述 reasoning，有独立证据通道。
6. State 是中心相关错误源，provenance 是中心点去相关机制。
7. `Completed = f(State, Evidence, Policy)`，≠ Agent 自报。
8. 学习环三表 + 双轨（统计轨/灾难轨）。
9. 构建顺序：控制面 → 观察 → 学习。

---

## 10. 产品战略关联（2026-08-15 深化）

APE 是**数据资产登记系统**（海南国际知识产权交易中心，GB/T 47950-2026）技术底座之一：

| APE 能力 | 登记系统用途 |
|------|------|
| 确定性控制面 + 状态机 | 登记流程编排 |
| 证据门禁 | 每环节合规审查（§7） |
| provenance + 机器时钟 | 不可伪造事件记录（§4.3 可追溯性） |

> 可追溯性完整解 = TDST HMAC 链（内容不可篡改）+ APE provenance/机器时钟（事件序列+时间不可伪造）→ 产出"不可伪造的登记证"。

战略方向：短期 = **GitHub 开源 APE + 技术长文**，不卖产品。

---

## 11. 踩坑记录

| # | 症状 | 根因 | 修复 |
|---|------|------|------|
| E2E-001 | 任务已认领无产出，cron 全跳过 | agent_poll.sh flock 僵尸锁（异常退出不清） | `rm -f /home/admin/.pipeline/locks/hermes.flock` |
| E2E-002 | 推进后下一站跳过 | claimed_by 状态推进不清零 | 推进时 `UPDATE SET claimed_by=NULL` |
| E2E-003 | cron 任务躺平 | PATH 不含 venv hermes | 用绝对路径 `/home/admin/.hermes/hermes-agent/venv/bin/hermes` |
| E2E-004 | waiting_human 群内无消息 | 状态变更不推送 | dispatch_loop 加状态变更推送 |
| E2E-005 | 签署 404 | 端点是 `/approve` 非 `/sign` | 用 `ctgc approve` 封装 |

**实战验证**：`e2e-demo-002` 五站全通（一次人类签署 + 四个自动接力），门禁当场抓获 DEMO 假完成。

---

## 12. 未解决 / 待办

1. 🔴 **最紧急**：CI-01 `/opt/pipeline/` 唯一裸代码副本无备份 → 先 scp 回本机 git 仓库备份。
2. 🔴 commit CI-01 Dispatcher 真实代码（main.py/models.py/gate_judge.py/config.json）回 git，再谈 push GitHub。
3. 学习环三问（留待实验）：谁生成候选 policy / 稀有灾难如何学习 / 如何验证 State 自身。
4. 故障注入两项未演：Agent 下线告警、超时降级链。
5. 登记系统定位已定 B（对外多租户），规划见 `~/数据资产登记系统/数据资产登记系统规划-v0.1-20260815.md`。

---

## 13. 材料来源清单

- 主 skill：`~/.hermes/skills/devops/agent-pipeline-engine/SKILL.md` + 4 个 references
- git 仓库：`~/agent-pipeline-engine/`（v0.1 死代码 + PROPOSAL-v5 + ADR-001）
- 记忆域文件：`~/.hermes/memory-files/ctgc.md`（管道顺序铁律/四门禁/观察窗）
- 实盘核查：CI-01 `123.56.25.232:/opt/pipeline/`（2026-08-22 SSH 核实）
- 过往会话：多 Agent 协同之争（8/15）、A2A 管道未通根因（8/11）、e2e-demo-002 试跑（8/11）
